{
  "name": "@textifai/payments",
  "version": "1.0.0",
  "main": "server.ts",
  "license": "MIT",
  "scripts": {
    "build": "npx tsc --build tsconfig-build.json",
    "start": "node dist/server.js",
    "test": "jest",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts"
  },
  "dependencies": {
    "stripe": "^14.2.0"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "jest": "^29.7.0",
    "ts-node-dev": "^2.0.0"
  }
}
